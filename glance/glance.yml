pages:
  - name: Startpage
    width: slim
    hide-desktop-navigation: true
    center-vertically: true
    columns:
      - size: full
        widgets:
          - type: search
            autofocus: true

          - type: monitor
            cache: 1m
            title: Services
            sites:
              - title: Home Assistant
                url: http://zero.local:8123/
                icon: si:homeassistant
              - title: Next cloud
                url: http://zero.local:8080
                icon: si:nextcloud
              - title: Next cloud
                url: http://zero.local:8080
                icon: si:nextcloud