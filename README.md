# home-server
My home server

Init command:
```
curl -fsSL https://raw.githubusercontent.com/danielmititelu/home-server/refs/heads/main/init.sh | bash
```

Commands:
```
cd /srv/compose
docker compose pull
docker compose up -d
```